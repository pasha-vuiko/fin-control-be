export interface IntervalJobScheduleParams {
  seconds?: number; // Interval in seconds
  minutes?: number; // Interval in minutes
  hours?: number; // Interval in hours
  days?: number; // Interval in days
  weeks?: number; // Interval in weeks
}
